import { EsfcurrencyPipe } from './esfcurrency.pipe';

describe('EsfcurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new EsfcurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
